{
  "name": "timetracking",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "ISC",
  "dependencies": {
    "@sap/cds": "^3.33.1",
    "@sap/xsenv": "^2.2.0",
    "@sap/xssec": "^2.2.5",
    "express": "^4.17.1",
    "hdb": "^0.17.1",
    "mbt": "^1.0.13",
    "passport": "^0.4.1"
  },
  "engines": {
    "node": "^10.18.0"
  },
  "scripts": {
    "deploy": "cds deploy",
    "start": "cds run",
    "build:mta": "cds build/all && cds deploy && mbt build -p=cf",
    "deploy:cf": "npm run build:mta && cf deploy mta_archives/timetracking_1.0.11.mtar"
  },
  "devDependencies": {
    "sqlite3": "^4.1.1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ],
        "credentials": {
          "database": "timetracking.db"
        },
        "[production]": {
          "kind": "hana"
        }
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv",
    "app"
  ]
}
