{
  "name": "timetracking",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "ISC",
  "dependencies": {
    "@sap/cds": "^3.20.1",
    "express": "^4.17.1",
    "hdb": "^0.17.1",
    "mbt": "^1.0.7",
    "passport": "^0.4.1"
  },
  "engines": {
    "node": "^8.9"
  },
  "scripts": {
    "build": "cds build/all --clean",
    "deploy": "cds deploy",
    "start": "cds run",
    "build:mta": "cds build/all && mbt build -p=cf",
    "deploy:cf": "npm run build:mta && cf deploy mta_archives/timetracking_1.0.4.mtar"
  },
  "devDependencies": {
    "sqlite3": "^4.1.1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ],
        "credentials": {
          "database": "timetracking.db"
        },
        "[production]": {
          "kind": "hana"
        }
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv"
  ]
}
